class Cart {
  final int id;
  final int user_id;
  final int product_id;
  final int quantity;

  Cart(
      {required this.id,
      required this.user_id,
      required this.product_id,
      required this.quantity});
}
